# Contributing

```bash
flutter pub get
flutter run -d <android-device>
```

The audio engine is C++ and needs the Android NDK that ships with Flutter /
Android Studio. arm64 devices are the intended target.

SideDeck is GNU GPL v3 or later. By sending a patch you license it under the
same terms.
